# Copy this file to .env and fill in the keys you have.
# The app works with any subset filled in — missing keys are just skipped
# in the fallback chain.

GROQ_API_KEY=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
HF_API_KEY=

# Optional, defaults to 5000
PORT=5000
